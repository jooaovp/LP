#include <stdio.h>
#include <math.h>

int main(){
	int t, v, saida;

	scanf("%d %d", &v, &t);

	do{
		saida = (t*2) * v;
		printf("%d\n", saida);
	} while ( scanf("%d %d", &v, &t) != EOF );

	return 0;
}
