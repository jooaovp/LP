#include <stdio.h>
#include <math.h>
int main ()
{
	int L, R, total;

	scanf("%d", &L);
	scanf("%d", &R);

    	while( (L!=0) && (R!=0))
		    {
			    total = L + R;
			    printf("%d\n", total);
			    scanf("%d", &L);
			    scanf("%d", &R);
		    }

	return 0;
}
